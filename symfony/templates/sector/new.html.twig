{% extends 'base.html.twig' %}

{% block title %}New Sector{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="e-panel card">
        <div class="card-body">
            <div class="card-title">
                <div class="row flex-lg-nowrap">
                    <div class="col">
                        <h2 class="mr-2"><span>Create New Sector</span></h2>
                    </div>
                </div>
            </div>
            <div class="e-table">
                {{ include('sector/_form.html.twig') }}
                <a href="{{ path('sector_index') }}" class="btn btn-md btn-primary mt-3">Cancel</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
